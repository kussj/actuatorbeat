################### Actuatorbeat Configuration Example #########################

############################# Actuatorbeat ######################################

actuatorbeat:
  # Defines how often an event is sent to the output
  period: 10s

  urls:
    - http://localhost:8080/metrics
